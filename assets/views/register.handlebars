<div class="flex flex-wrap justify-center items-center gap-5">
	<div class="text-muted-foreground text-lg mb-4 w-sm flex flex-col justify-between">
	{{> logo}}

	<div>
		<p>Shoot is a federated instant messenger.</p>
		<p>
			Connect with your friends on Shoot or the rest of the Fediverse with Group Chats, Direct
			Messages, and Guilds.
		</p>
	</div>
	{{#if server.terms}}
		<div>
			<p>
				By joining this server, you agree to its
			</p>
			<a href="{{server.terms}}" target="_blank" class="underline">Terms of Service</a>
		</div>
	{{/if}}
</div>

<div
	class="w-sm bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 px-6 shadow-sm"
>
	<h1 class="leading-none font-semibold">
		Register
	</h1>

	<div id="errors" class="hidden"></div>

	<form method="post" class="space-y-4">
		{{#if disabled}}
			<label
				class="flex gap-2 text-sm leading-none select-none"
				for="invite"
			>
				Registration Token
			</label>
			<p class="text-muted-foreground text-sm">
				This instance has closed registration. You may use a registration token to join.
			</p>
			<input
				type="text"
				id="invite"
				name="invite"
				required
				class="selection:bg-primary selection:text-primary-foreground dark:bg-input/30 border-input flex h-9 w-full min-w-0 rounded-md border bg-transparent px-3 py-1 text-base shadow-xs transition-[color,box-shadow] outline-none md:text-sm focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]"
			/>
		{{/if}}

		<label
			class="flex gap-2 text-sm leading-none select-none"
		>
			Username
		</label>
		<input
			type="text"
			id="username"
			name="username"
			required
			class="selection:bg-primary selection:text-primary-foreground dark:bg-input/30 border-input flex h-9 w-full min-w-0 rounded-md border bg-transparent px-3 py-1 text-base shadow-xs transition-[color,box-shadow] outline-none md:text-sm focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]"
		/>

		<label
			class="flex gap-2 text-sm leading-none select-none"
		>
			Password
		</label>
		<input
			type="password"
			id="password"
			name="password"
			required
			class="selection:bg-primary selection:text-primary-foreground dark:bg-input/30 border-input flex h-9 w-full min-w-0 rounded-md border bg-transparent px-3 py-1 text-base shadow-xs transition-[color,box-shadow] outline-none md:text-sm focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]"
		/>

		<button
			type="submit"
			name="allow"
			value="true"
			class="cursor-pointer inline-flex items-center justify-center gap-2 rounded-md text-sm shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] bg-primary text-primary-foreground shadow-xs hover:bg-primary/90 h-9 px-4 py-2"
		>
			Register
		</button>
	</form>
</div>
</div>